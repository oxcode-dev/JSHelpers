import { describe, expect, it } from "vitest";
import { randomStr } from "../src/helper";

describe('randomStr', () => { 
    it('should return ', () => {
        expect(randomStr()).toBe(2)
    })
})  